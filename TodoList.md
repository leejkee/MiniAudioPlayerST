# Todo List

## VS1003

- [ ] 将 MP3 音频数据传输由阻塞式 SPI2 改为 DMA。
  - SCI 寄存器读写继续使用阻塞方式，仅将持续占用总线的 SDI 音频数据发送迁移到 DMA。
  - 保持 DREQ 流控：DREQ 有效且 DMA 空闲时启动一次发送，单次数据长度不超过 VS1003 允许的 32 字节。
  - 明确 `XDCS` 在 DMA 启动、完成、错误和中止路径中的拉低/释放时机，避免与 SCI 的 `XCS` 事务重叠。
  - 增加 DMA busy、完成和错误状态，由完成回调推进播放器缓冲区偏移，禁止播放器在 DMA 完成前改写正在发送的数据。
  - 评估使用 512 字节文件缓冲加两个 32 字节发送缓冲的 ping-pong 方案，避免 FatFS 读取与 SPI2 DMA 争用同一片数据。
  - 上机记录 DREQ 等待、DMA 错误和缓冲欠载次数，验证切歌、暂停、曲末补零及连续播放路径。

- [ ] 将 VS1003 诊断功能从正式播放驱动中拆分。
  - 将 `BSP_VS1003_SineTest()` 以及后续的 SCI 稳定性、存储器等测试迁移到独立的 BSP 诊断模块，例如 `BSP/test/bsp_vs1003_diag.c`。
  - 保持 SPI、XCS、XDCS、DREQ 和复位等底层硬件操作封装在 BSP 内，不让 `App/test` 直接操作硬件。
  - 增加类似 `BSP_VS1003_ENABLE_DIAGNOSTICS` 的条件编译选项，使正式生产固件可以排除诊断代码。
  - 在 VS1003 硬件验证完成并进入正式功能集成阶段后实施。

## OLED 与播放并发性能

- [ ] 降低 OLED 常规刷新频率，避免 I2C 阻塞影响 MP3 数据供给。
  - 播放进度和解码时间采用 1.5–2 秒刷新周期，默认先按 2 秒实现并通过上机测试调整。
  - 按键按下/释放、播放状态、切歌、音量和页面切换仍允许即时刷新，但需要合并同一轮事件产生的重复刷新。
  - 拆分“清显存”和“发送显存”：当前 `BSP_SSD1315_Clear()` 会立即刷屏，页面随后再次 `Refresh()` 会造成一次界面更新发送两遍完整显存。
  - 保留 dirty-page 更新，只发送实际变化的 OLED page；评估主界面时间变化时仅重绘并刷新最后两页。
  - 当 VS1003 DREQ 等待发送或 SPI2 DMA 正在调度下一块数据时，延后非紧急 OLED 刷新。
  - 上机测量一次全屏/局部 OLED 刷新的耗时，并验证刷新期间无音频卡顿、DMA 欠载或进度显示异常。
