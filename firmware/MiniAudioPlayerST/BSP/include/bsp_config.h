/**
  ******************************************************************************
  * @file    bsp_config.h
  * @brief   BSP 编译时配置 — 集中管理所有 BSP 模块的开关与参数
  * @note    此文件不在 CubeMX 管理范围内, 重新生成代码不会被覆盖。
  ******************************************************************************
  */

#ifndef __BSP_CONFIG_H__
#define __BSP_CONFIG_H__

#ifdef __cplusplus
extern "C" {
#endif

/* Includes ------------------------------------------------------------------*/
#include "main.h"

/* ========================================================================= */
/*                          调试串口 (USART Debug)                            */
/* ========================================================================= */

/**
  * @brief  调试串口总开关
  *         1 = 启用 printf 重定向, 0 = 禁用 (fputc 不编译, 调试打印宏展开为空)
  * @note   USART2 由 CubeMX 配置为常驻外设, 始终初始化, 不受此开关影响。
  *         波特率等硬件参数在 CubeMX .ioc 中管理。
  */
#define BSP_DEBUG_UART              1


/* ========================================================================= */
/*                         调试日志等级 (可选扩展)                            */
/* ========================================================================= */

/**
  * @brief  日志等级: 0=NONE  1=ERROR  2=WARN  3=INFO  4=DEBUG
  * @note   当前仅用作预留设计, 具体过滤逻辑在 bsp_uart.c 中实现
  */
#define BSP_DEBUG_LEVEL             4


/* ========================================================================= */
/*                          调试打印宏                                        */
/* ========================================================================= */

#if BSP_DEBUG_UART
  /* ---- 启用时: 走 printf ---- */
  #include <stdio.h>
  #define BSP_DEBUG_PRINTF(fmt, ...)   printf(fmt, ##__VA_ARGS__)
#else
  /* ---- 禁用时: 宏展开为空, 参数不会被求值 ---- */
  #define BSP_DEBUG_PRINTF(fmt, ...)   ((void)0)
#endif


#ifdef __cplusplus
}
#endif

#endif /* __BSP_CONFIG_H__ */
